// @emr/shell is an application, not a library. The Vite entry is
// `index.html` → `src/main.tsx`. Nothing imports `@emr/shell` (the
// boundaries plugin forbids it), so this barrel is intentionally empty.
export {};
